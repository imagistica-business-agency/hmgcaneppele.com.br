import styled from 'styled-components'

export const DiferencialWrapper = styled.section`
  display: grid;
  grid-template-columns: 2fr 1fr 1fr;
  gap: 20px;
`

export const DiferencialTitle = styled.h3`
  color: #181a48;
  font-family: 'Poppins', Sans-serif;
  font-size: 24px;
  line-height: 1.3em;
  font-weight: 400;
  letter-spacing: 0.5px;
`
